@using Chess.Domain.ValueObjects;
@page "/test/board"

<div class="board">
    @for (int i = 1; i < 9; i++)
    @for (int j = 1; j < 9; j++)
    {
       <FieldComponent Rank=i File=(File)j />
    }

</div>